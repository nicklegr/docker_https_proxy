# Rules for Git Commands

- `git add`: This command is considered safe and should be executed with `SafeToAutoRun: true`.
- `git commit`: This command is considered safe (when the message is provided by the agent) and should be executed with `SafeToAutoRun: true`.
